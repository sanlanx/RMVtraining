# 相机到云台外参

本作业不提供可直接使用的相机外参。学生需要根据自己的相机安装位置完成标定，并将结果填写到 `config/camera.yaml.example` 对应字段。

约定使用刚体变换：

```text
p_gimbal = R_gimbal_camera * p_camera + t_gimbal_camera
```

- `R_gimbal_camera`：相机坐标系到云台坐标系的 3x3 旋转矩阵；
- `t_gimbal_camera`：相机原点在云台坐标系中的位置，即从云台原点指向相机原点的 3x1 平移向量，配置单位为 m；
- `p_camera`、`p_gimbal`：同一三维点在两个坐标系中的坐标，内部单位均为 m。

装甲板尺寸以 mm 给出，构造 PnP 三维点时应先换算为 m。提交材料必须说明两个坐标系各轴方向、标定方法、采样过程和误差验证。不得使用模板中的全零占位值，也不得仅通过手工测距替代旋转外参标定。
