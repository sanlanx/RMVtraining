# 环境依赖

环境配置是大作业内容之一。仓库只提供最小 CMake，不提供一键安装脚本、容器、预配置工具链或第三方库安装包。

## 基础依赖

- Linux 开发环境；
- 支持 C++17 的编译器；
- CMake；
- OpenCV；
- Eigen3；
- POSIX Threads。

## 按实现选择的依赖

- 海康相机取流：HIKROBOT MVS SDK；
- 神经网络路线：学生选择的推理运行时及其离线模型；
- 传统视觉数字分类：学生选择的分类器或 OpenCV DNN 运行时；
- 其他优化、日志或数学库：仅在实际代码使用时添加。

学生需要自行完成安装、版本选择、CMake 查找、动态库路径配置和设备权限设置。不得依赖验收现场联网下载组件。

## 验收前自查

```bash
cmake -S . -B build-rmvtraining
cmake --build build-rmvtraining -j
```

此外应确认：

- 相机 SDK 示例能够发现设备并取流；
- 当前用户有串口和相机访问权限；
- 模型、标签、配置等运行资源使用可复现路径；
- 从新的构建目录开始不依赖 IDE 私有配置；
- 报告中列出系统、编译器和主要依赖版本。
