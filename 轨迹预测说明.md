# 旋转车体 EKF 预测要求

学生需要自行完成适用于装甲板自瞄的旋转车体 EKF。仓库中的 `BulletModel`、`KalmanFilter` 和 `Planner` 仅供理解与选用，不代表完整答案，也不要求原样使用。

状态模型至少应能表达：

- 车体旋转中心的位置和速度；
- 车体 yaw 与 yaw 角速度；
- 装甲板到旋转中心的半径；
- 不同装甲板可能存在的高度差或交替半径。

实现必须处理初始化、观测更新、角度跨越 `-pi/pi`、装甲板切换、离群观测、短时丢失和长时间丢失重置。预测时间应包含实际测得的处理延迟；使用弹道飞行时间时，应在报告中说明单位、模型和参数来源。

输出到通信结构体时：

- `crc=0`：没有可用 EKF 目标；
- `crc=1`：预测稳定；
- `crc=2`：已有目标但预测尚不稳定；
- `crc!=1` 时必须令 `fire_allowance=0`。

Web Debug 至少记录车体中心、平移速度、yaw、yaw 角速度、旋转半径和 EKF 状态，便于展示收敛、切换及遮挡恢复过程。
