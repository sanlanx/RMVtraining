# 64 字节自瞄通信结构体

本作业沿用原通信结构体。不得修改字段名称、类型、顺序、对齐方式或 64 字节总长度。结构体按 1 字节对齐，并通过同一个 64 字节缓冲区完成串口收发。

> `crc` 是历史字段名，本作业中表示 EKF 状态，不是 CRC 校验和。不得为该字段新增校验算法。

```cpp
#pragma pack(push, 1)
struct MessData_AutoAim {
  uint8_t head;          // 0x71

  // 电控发送给视觉
  float yaw;             // 当前云台 yaw，rad
  float pitch;           // 当前云台 pitch，rad
  float roll;            // 当前云台 roll，rad
  uint8_t status;        // 自瞄状态与己方颜色
  uint8_t is_far;        // 原协议字段，含义由电控约定

  uint8_t armor_flag;    // 目标车编号，0 表示无目标
  float latency;         // 视觉处理延迟，ms
  float bias;            // 电控给出的预测时间偏置，s
  float distance;        // 目标距离，m
  float pitch_offset;    // 原协议保留字段
  float coo_x;           // EKF 车体中心 x，mm
  float coo_y;           // EKF 车体中心 y，mm
  float wheel_w;         // 原协议保留字段
  uint8_t fire_allowance;// 0 禁止开火，1 允许开火
  float target_yaw;      // 目标 yaw，rad
  float target_pitch;    // 目标 pitch，rad
  float yaw_vel;         // 目标 yaw 角速度，rad/s
  float pitch_vel;       // 目标 pitch 角速度，rad/s
  uint16_t crc;          // EKF 状态：0 无目标，1 稳定，2 不稳定
  uint8_t tail;          // 0x4C
};
#pragma pack(pop)

static_assert(sizeof(MessData_AutoAim) == 64);
```

## 字节布局

| 偏移 | 字段 | 类型 | 方向 |
| ---: | --- | --- | --- |
| 0 | `head` | `uint8_t` | 双方维护 |
| 1 | `yaw` | `float` | 电控 -> 视觉 |
| 5 | `pitch` | `float` | 电控 -> 视觉 |
| 9 | `roll` | `float` | 电控 -> 视觉 |
| 13 | `status` | `uint8_t` | 电控 -> 视觉 |
| 14 | `is_far` | `uint8_t` | 电控 -> 视觉 |
| 15 | `armor_flag` | `uint8_t` | 视觉 -> 电控 |
| 16 | `latency` | `float` | 视觉 -> 电控 |
| 20 | `bias` | `float` | 电控 -> 视觉 |
| 24 | `distance` | `float` | 视觉 -> 电控 |
| 28 | `pitch_offset` | `float` | 保留 |
| 32 | `coo_x` | `float` | 视觉 -> 电控 |
| 36 | `coo_y` | `float` | 视觉 -> 电控 |
| 40 | `wheel_w` | `float` | 保留 |
| 44 | `fire_allowance` | `uint8_t` | 视觉 -> 电控 |
| 45 | `target_yaw` | `float` | 视觉 -> 电控 |
| 49 | `target_pitch` | `float` | 视觉 -> 电控 |
| 53 | `yaw_vel` | `float` | 视觉 -> 电控 |
| 57 | `pitch_vel` | `float` | 视觉 -> 电控 |
| 61 | `crc` | `uint16_t` | 视觉 -> 电控 |
| 63 | `tail` | `uint8_t` | 双方维护 |

## 本作业使用的状态

| `status` | 己方颜色 | 行为 |
| ---: | --- | --- |
| 0 | 红色 | 装甲板自瞄 |
| 5 | 蓝色 | 装甲板自瞄 |

其他 `status` 值不属于本作业范围。程序收到时应输出安全状态，不实现其他模式分支。

## 安全输出

没有有效目标、PnP 失败、结果过期、出现 NaN/Inf 或 EKF 失效时，至少应设置：

```text
armor_flag = 0
fire_allowance = 0
distance = 0
coo_x = 0
coo_y = 0
yaw_vel = 0
pitch_vel = 0
crc = 0
```

EKF 已有目标但尚未稳定时使用 `crc=2` 且 `fire_allowance=0`；只有稳定状态 `crc=1` 才能进一步判断是否允许开火。
